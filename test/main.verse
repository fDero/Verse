return "ciao";

defer close(file.fileptr);

continue;

break;

attempt open(file);

break;

attempt open(file);
attempt resolve(file);
default kill();

#file.filename + 3;
3 + 4 * 5 + 6;